services:
  - type: web
    name: cvformatic-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    preBuildCommand: apt-get update && apt-get install -y tesseract-ocr
